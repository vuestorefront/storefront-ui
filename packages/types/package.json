{
  "name": "@sfui/types",
  "version": "0.0.0",
  "private": true,
  "types": "lib/index.d.ts",
  "type": "module",
  "scripts": {
    "dev": "nodemon",
    "build": "yarn generate-ts",
    "generate-ts": "rm -rf lib && npx -p typescript tsc --declaration --emitDeclarationOnly --outDir lib"
  }
}
