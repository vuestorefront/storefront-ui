export enum VsfAlertVariant {
  neutral = 'neutral',
  secondary = 'secondary',
  positive = 'positive',
  warning = 'warning',
  error = 'error',
}

export enum VsfAlertType {
  temporary = 'temporary',
  persistent = 'persistent',
}
