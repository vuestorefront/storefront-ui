export enum VsfAlertVariants {
  gray = 'gray',
  secondary = 'secondary',
  positive = 'positive',
  warning = 'warning',
  error = 'error',
}

export enum VsfAlertTypes {
  temporary = 'temporary',
  persistent = 'persistent',
}
