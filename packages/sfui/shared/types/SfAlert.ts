export enum SfAlertVariant {
  neutral = 'neutral',
  secondary = 'secondary',
  positive = 'positive',
  warning = 'warning',
  error = 'error',
}

export enum SfAlertType {
  temporary = 'temporary',
  persistent = 'persistent',
}
