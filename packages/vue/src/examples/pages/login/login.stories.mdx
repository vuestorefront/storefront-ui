import { Meta, Props, Preview, Story } from '@storybook/addon-docs/blocks';

import { story, params } from '~storybook-util/story';
import LoginComponent from './Login'

<Meta title="Pages/Login"
  component={LoginComponent}
/>

# Login example page 


<Preview>
  <Story 
    name='Login' 
    inline 
    parameters={params(LoginComponent)}
  >
    {story(LoginComponent)}
  </Story>
</Preview>