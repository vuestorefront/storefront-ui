import { Meta, Props, Preview, Story } from "@storybook/addon-docs/blocks";

import { story, params } from "~storybook-util/story";
import CartComponent from "./Cart";

<Meta title="Components/Templates/Cart" component={CartComponent} />

# Cart example page

<Preview>
  <Story name="Cart" inline parameters={params(CartComponent)} height="700px">
    {story(CartComponent)}
  </Story>
</Preview>
