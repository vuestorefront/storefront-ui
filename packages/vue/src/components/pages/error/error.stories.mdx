import { Meta, Props, Preview, Story } from '@storybook/addon-docs/blocks';

import { story, params } from '~storybook-util/story';
import ErrorComponent from './Error'

<Meta title="Components/Pages/Error"
  component={ErrorComponent}
/>

# Error example page 


<Preview>
  <Story 
    name='Error' 
    inline 
    parameters={params(ErrorComponent)}
  >
    {story(ErrorComponent)}
  </Story>
</Preview>