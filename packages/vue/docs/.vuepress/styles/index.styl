.navbar .site-name.can-hide {
  display: none;
}

a {
  color:
}

.theme-default-content:not(.custom){
  max-width: 1140px;
}
.vuepress-mobile-checkbox {
  margin: .5rem 0 .5rem .5rem;
}
.vuepress-mobile-label {
  margin: .5rem .5rem .5rem 0;
}
#vuepress-mobile:checked + .storybook-iframe {
  display: block;
  max-width: 1023px;
  overflow: hidden;
}
.vuepress-mobile{
  display: flex;
  align-items center;
  flex-wrap wrap;
}
.storybook-iframe {
  flex: 0 0 100%;
}