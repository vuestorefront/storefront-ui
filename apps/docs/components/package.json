{
  "name": "@sfui/docs",
  "version": "0.0.0",
  "private": true,
  "main": "index.md",
  "scripts": {
    "dev": "concurrently \"vitepress dev --port=8000\" \"yarn watch-docs\"",
    "build": "yarn update-docs && vitepress build",
    "watch-docs": "nodemon",
    "serve": "vitepress serve",
    "update-docs": "yarn node ./utils/createFrameworksDocs"
  },
  "devDependencies": {
    "@sfui/sfui": "*",
    "tailwindcss": "^3.1.8",
    "vitepress": "^1.0.0-alpha.4"
  }
}
